# Illustrative, not normative. The release manifest that the example Task pins to.
# Every Task records the release_manifest_id it ran against; this is what makes
# the platform reproducible under change.

release_manifest_id: rm_2026_05_15_a
tenant_id: tenant_acme

platform:
  git_tag: v0.4.2
  task_contract_version: 1
  notes: |
    First release after the side-by-side versioning landing. No contract-breaking changes.

prompts:
  - id: pt_extract_action_items
    version: 5
  - id: pt_slack_confirmation
    version: 2
  - id: pt_grounded_monday_review
    version: 3

routines:
  - id: rt_tldv_to_monday
    version: 2
    pinned: true
  - id: rt_slack_artifact_to_sheet
    version: 1
  - id: rt_monday_review_to_slack
    version: 1

policies:
  - id: pol_external_customer_writes
    version: 3
  - id: pol_default_pii
    version: 7
  - id: pol_egress_third_party
    version: 2

tool_plan:
  id: tp_acme_2026_q2
  version: 4

evaluator:
  criteria_suites:
    - id: cs_tldv_to_monday
      version: 4
    - id: cs_slack_artifact_to_sheet
      version: 2
    - id: cs_monday_review_to_slack
      version: 1

created_at: "2026-05-15T09:00:00Z"
created_by:
  kind: human
  id: U07RELEASE
notes_for_humans: |
  Adds pol_external_customer_writes v3 (was v2) — tightens agentic judge prompt.
  Pins rt_tldv_to_monday at v2 because tenant_acme is in the v3 evaluation window.
